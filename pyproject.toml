[project]
name = "tgmirror"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyrofork",
    "tgcrypto",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff",
]
