[project]
name = "tgmirror"
version = "2026.03.05"
requires-python = ">=3.11"
dependencies = [
    "pyrofork",
    "tgcrypto",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff",
]
